# Scope

> Use this to limit scope creep. Update it when the team locks the version-1 backlog.

## In scope — initialization (now)

- Standard folders for docs, frontend, backend, and tests.
- Baseline docs: vision, scope, conventions, docs index.
- Branch / commit / pull-request conventions.

## In scope — version 1 (planned, not locked)

Fill this in after the feature list exists. Suggested frame:

- Accounts / roles if needed
- Core features that match the GeoSound idea
- Minimum data storage for a demo
- Tests for the main flows

## Out of scope (for now)

- Choosing a stack and scaffolding the app (implementation step).
- Detailed requirements, use cases, mockups (`docs/01-requirements/`).
- Architecture / database / API design (`docs/02-design/`).
- CI/CD, production infrastructure, large-scale performance work — unless the course requires them.

## Known constraints

- Course: Software Engineering 2, Saigon University.
- GitHub remote: `Tran-Gia-Phuc/GeoSound`.
- Long-lived branches already exist: `main`, `develop`, `staging`.
