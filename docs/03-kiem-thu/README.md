# 03 — Kiểm thử

Thư mục dành cho chiến lược và ca kiểm thử.

Dự kiến sẽ có:

- Kế hoạch kiểm thử
- Test case cho luồng chính
- Ghi chú kết quả / bug

Chưa viết ở bước khởi tạo. Mã kiểm thử tự động đặt ở `tests/` hoặc cạnh mã nguồn — ghi chú tại đây khi đã chốt.
